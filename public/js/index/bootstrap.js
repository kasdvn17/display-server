  /* Keep Spotify playback alive across tabs. Home/News only hide the Media UI;
     the photo/news timing remains independent from Spotify playback state. */
  /* run entrance animation for the initially active view */
  animateEntrance("home");

})();
